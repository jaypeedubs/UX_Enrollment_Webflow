# ============================================================
# ICIT Application System — Environment Variables
# Copy to .env and fill in real values. Never commit .env.
# ============================================================

# --- Supabase (client-safe: anon key only) ---
SUPABASE_URL=
SUPABASE_ANON_KEY=

# --- Supabase (server-side only — never expose to client) ---
SUPABASE_SERVICE_ROLE_KEY=

# --- Resend ---
RESEND_API_KEY=

# --- Stripe ---
STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# --- Moodle (TBD — request credentials in Phase 8) ---
MOODLE_BASE_URL=
MOODLE_API_TOKEN=

# --- App ---
SITE_URL=
